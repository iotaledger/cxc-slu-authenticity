import { ClientConfig } from '../models/clientConfig';
/**
 * This is the base client used as a parent class for all clients
 * using the integration services api.
 */
export declare abstract class BaseClient {
    private apiKey;
    private baseUrl;
    private apiVersion;
    private jwtToken?;
    private instance;
    constructor({ apiKey, baseUrl, apiVersion }?: ClientConfig);
    /**
     * Authenticates the user to the api for requests where authentication is needed
     * @param id of the user to authenticate
     * @param secretKey of the user to authenticate
     */
    authenticate(id: string, secretKey: string): Promise<void>;
    private signNonce;
    private hashNonce;
    private getHexEncodedKey;
    protected post(url: string, data: any): Promise<any>;
    protected get(url: string, params?: any): Promise<any>;
    protected delete(url: string, params?: any): Promise<any>;
    protected put(url: string, data: any): Promise<any>;
}
//# sourceMappingURL=base.d.ts.map